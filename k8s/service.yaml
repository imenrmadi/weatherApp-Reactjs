apiVersion: v1
kind: Service
metadata:
  name: weatherapp
  namespace: default
  labels:
    app: weatherapp
spec:
  selector:
    app: weatherapp
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
